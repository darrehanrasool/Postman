$kind: http-request
name: Grocery Store API Delete an item in the cart
url: "{{address}}/carts/:cartId/items/:itemId"
method: DELETE
pathVariables:
  cartId: "{{cartId}}"
  itemId: " 432001206"
body:
  type: json
  content: ""
auth:
  credentials:
    token: "{{accessToken}}"
  type: bearer
order: 4484485806990072
