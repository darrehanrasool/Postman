$kind: http-request
name: Store API  Get all orders
url: "{{address}}/orders"
method: GET
body:
  type: json
  content: ""
auth:
  credentials:
    - key: token
      value: "{{accessToken}}"
  type: bearer
order: 4489879848977365
