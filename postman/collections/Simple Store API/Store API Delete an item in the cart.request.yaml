$kind: http-request
name: Store API Delete an item in the cart
url: "{{address}}/carts/:cartId/items/:itemId"
method: DELETE
pathVariables:
  - key: cartId
    value: "{{cartId}}"
  - key: itemId
    value: " 432001206"
body:
  type: json
  content: ""
auth:
  credentials:
    - key: token
      value: "{{accessToken}}"
  type: bearer
order: 4484485806990072
